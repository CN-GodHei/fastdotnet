# 通用事件总线系统 - PluginA 演示插件

## 📦 项目概述

PluginA 作为**演示插件**，展示了如何在 Fastdotnet 插件系统中使用通用事件总线实现：
- ✅ **事件发布** - 业务操作触发事件通知其他插件
- ✅ **事件订阅** - 响应其他插件的事件（如支付完成、插件安装）
- ✅ **完全解耦** - 插件之间通过事件总线通信，无需直接依赖

---

## 🏗️ 架构层次

```
┌─────────────────────────────────────────┐
│      PluginA (演示插件)                  │
│                                         │
│  ┌──────────────┐                      │
│  │ Events/      │ ← 定义业务事件        │
│  │ - MallOrderCreatedEvent.cs          │
│  └──────────────┘                      │
│                                         │
│  ┌──────────────┐                      │
│  │ EventHandlers/│ ← 订阅其他插件事件   │
│  │ - OrderPaidEventHandler.cs          │
│  │ - PluginInstalledEventHandler.cs    │
│  └──────────────┘                      │
│                                         │
│  ┌──────────────┐                      │
│  │ Services/    │ ← 发布事件的业务逻辑  │
│  │ - MallOrderService.cs               │
│  └──────────────┘                      │
│                                         │
│  ┌──────────────┐                      │
│  │ Controllers/ │ ← API 接口            │
│  │ - MallOrderController.cs            │
│  └──────────────┘                      │
└─────────────────────────────────────────┘
              ↓ uses
┌─────────────────────────────────────────┐
│  Fastdotnet.Plugin.Contracts.Events     │
│  - IEventBus                            │
│  - IEventHandler<TEvent>                │
│  - EventBase / DomainEvent              │
│  - PaymentEvents / SystemEvents         │
└─────────────────────────────────────────┘
```

---

## 📁 文件清单

### 1. 事件定义 (`Events/`)

#### MallOrderCreatedEvent.cs
```csharp
using Fastdotnet.Plugin.Contracts.Events;

namespace Fastdotnet.PluginA.Events;

/// <summary>
/// 商城订单创建事件
/// </summary>
[Event(Name = "MallOrderCreated", Description = "商城订单创建", Group = "Mall")]
public class MallOrderCreatedEvent : DomainEvent
{
    public override string AggregateId => OrderId;
    
    public string OrderId { get; set; } = string.Empty;
    public string UserId { get; set; } = string.Empty;
    public decimal TotalAmount { get; set; }
    public int ItemCount { get; set; }
    public Dictionary<string, object> OrderItems { get; set; } = new();
}
```

**关键点**：
- ✅ `using Fastdotnet.Plugin.Contracts.Events;` - 必须引用
- ✅ 继承 `DomainEvent` - 领域事件基类
- ✅ `[Event]` 特性标记 - 用于自动发现
- ✅ `override AggregateId` - 指定聚合根 ID

---

### 2. 事件处理器 (`EventHandlers/`)

#### OrderPaidEventHandler.cs
```csharp
using Fastdotnet.Plugin.Contracts.Events;

namespace Fastdotnet.PluginA.EventHandlers;

/// <summary>
/// 支付完成事件处理器
/// </summary>
public class OrderPaidEventHandler : IEventHandler<PaymentCompletedEvent>
{
    private readonly ILogger<OrderPaidEventHandler> _logger;
    
    public OrderPaidEventHandler(ILogger<OrderPaidEventHandler> logger)
    {
        _logger = logger;
    }
    
    public async Task HandleAsync(PaymentCompletedEvent @event, CancellationToken cancellationToken = default)
    {
        _logger.LogInformation("收到支付完成事件：订单 ID={OrderId}, 金额={Amount:C}", 
            @event.OrderId, @event.PaidAmount);
        
        // TODO: 业务逻辑
        await Task.CompletedTask;
    }
}
```

**关键点**：
- ✅ 实现 `IEventHandler<PaymentCompletedEvent>` 接口
- ✅ 依赖注入支持（ILogger）
- ✅ 异步处理方法
- ✅ 自动被事件总线发现和调用

---

### 3. 服务层 (`Services/`)

#### MallOrderService.cs
```csharp
using Fastdotnet.Plugin.Contracts.Events;
using Fastdotnet.PluginA.Events;

namespace Fastdotnet.PluginA.Services;

/// <summary>
/// 商城订单服务（演示发布事件）
/// </summary>
public class MallOrderService
{
    private readonly IEventBus _eventBus;
    private readonly ILogger<MallOrderService> _logger;
    
    public MallOrderService(IEventBus eventBus, ILogger<MallOrderService> logger)
    {
        _eventBus = eventBus;
        _logger = logger;
    }
    
    public async Task<string> CreateOrderAsync(string userId, decimal totalAmount, int itemCount)
    {
        var orderId = $"ORD_{Guid.NewGuid():N}";
        
        // 保存订单...
        await Task.Delay(100);
        
        // 发布订单创建事件
        await _eventBus.PublishAsync(new MallOrderCreatedEvent
        {
            OrderId = orderId,
            UserId = userId,
            TotalAmount = totalAmount,
            ItemCount = itemCount,
            Source = "PluginA"
        });
        
        return orderId;
    }
}
```

**关键点**：
- ✅ 注入 `IEventBus` - 用于发布事件
- ✅ 创建事件对象并填充数据
- ✅ 调用 `PublishAsync()` 发布事件

---

### 4. 控制器 (`Controllers/`)

#### MallOrderController.cs
```csharp
namespace Fastdotnet.PluginA.Controllers;

/// <summary>
/// 商城订单控制器
/// </summary>
[ApiController]
[Route("api/pluginA/[controller]")]
public class MallOrderController : ControllerBase
{
    private readonly MallOrderService _orderService;
    
    [HttpPost("create")]
    public async Task<IActionResult> CreateOrder([FromBody] CreateOrderRequest request)
    {
        var orderId = await _orderService.CreateOrderAsync(
            request.UserId, 
            request.TotalAmount, 
            request.ItemCount);
        
        return Ok(new { success = true, orderId });
    }
}
```

---

### 5. 插件主类 (`PluginA.cs`)

```csharp
public void ConfigureServices(ContainerBuilder builder)
{
    // ========== 注册事件处理器（订阅其他插件的事件） ==========
    builder.RegisterType<EventHandlers.OrderPaidEventHandler>()
        .As<Fastdotnet.Plugin.Contracts.Events.IEventHandler<Fastdotnet.Plugin.Contracts.Events.PaymentCompletedEvent>>()
        .InstancePerLifetimeScope();
    
    builder.RegisterType<EventHandlers.PluginInstalledEventHandler>()
        .As<Fastdotnet.Plugin.Contracts.Events.IEventHandler<Fastdotnet.Plugin.Contracts.Events.PluginInstalledEvent>>()
        .InstancePerLifetimeScope();
    
    // ========== 注册服务（用于发布事件） ==========
    builder.RegisterType<Services.MallOrderService>().AsSelf().InstancePerLifetimeScope();
}
```

**关键点**：
- ✅ 事件处理器必须注册到 DI 容器
- ✅ 使用 `As<IEventHandler<TEvent>>()` 指定接口
- ✅ 事件总线会自动发现并订阅

---

## 🔧 项目文件配置

### PluginA.csproj
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- ✅ 必须引用契约层 -->
    <ProjectReference Include="..\..\Fastdotnet.Plugin.Contracts\Fastdotnet.Plugin.Contracts.csproj" />
    <ProjectReference Include="..\..\Fastdotnet.Core\Fastdotnet.Core.csproj" />
    <ProjectReference Include="..\..\Fastdotnet.Orm\Fastdotnet.Orm.csproj" />
  </ItemGroup>
</Project>
```

---

## 🧪 测试流程

### 步骤 1: 启动主应用
```bash
cd D:\GodHeiWorkSpace\开源项目开发\Fastdotnet\backend\Fastdotnet.WebApi
dotnet run
```

### 步骤 2: 创建订单并发布事件
```bash
curl -X POST https://localhost:5001/api/pluginA/mallOrder/create \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "user123",
    "totalAmount": 199.99,
    "itemCount": 2
  }'
```

### 预期日志输出
```
【PluginA】创建订单：订单 ID=ORD_xxx, 金额=¥199.99
【PluginA】订单创建成功，已发布 MallOrderCreatedEvent 事件
```

### 步骤 3: 验证事件订阅
当支付插件发布 `PaymentCompletedEvent` 时：
```
【PluginA】收到支付完成事件：订单 ID=ORD_xxx, 金额=¥199.99
【PluginA】支付完成事件处理完成
```

---

## 🎯 事件流转示意图

```
User Request
    ↓
MallOrderController.create()
    ↓
MallOrderService.CreateOrderAsync()
    ├─→ 保存订单到数据库
    └─→ 发布 MallOrderCreatedEvent
            ↓
        InMemoryEventBus.PublishAsync()
            ↓
        遍历所有订阅者
            ├─→ InventoryHandler (库存插件)
            ├─→ EmailHandler (邮件插件)
            └─→ NotificationHandler (通知插件)
```

---

## 📊 事件类型对比

| 事件名称 | 类型 | 发布者 | 订阅者 | 用途 |
|---------|------|--------|--------|------|
| **MallOrderCreated** | DomainEvent | PluginA | 其他插件 | 通知订单创建 |
| **PaymentCompleted** | DomainEvent | 支付插件 | PluginA | 通知支付完成 |
| **PluginInstalled** | IntegrationEvent | 主应用 | 所有插件 | 通知插件安装 |

---

## ✅ 最佳实践检查清单

### 事件定义
- [x] 添加 `using Fastdotnet.Plugin.Contracts.Events;`
- [x] 继承正确的基类（DomainEvent / IntegrationEvent）
- [x] 添加 `[Event]` 特性标记
- [x] `override AggregateId` 指定聚合根 ID
- [x] 包含必要的业务数据（不超过 10 个属性）

### 事件处理器
- [x] 实现 `IEventHandler<TEvent>` 接口
- [x] 使用依赖注入（ILogger、IService 等）
- [x] 异步方法签名 `Task HandleAsync(...)`
- [x] 在 DI 容器中注册
- [x] 异常处理（不抛出异常）

### 事件发布
- [x] 注入 `IEventBus` 接口
- [x] 创建事件对象并填充数据
- [x] 调用 `PublishAsync()` 发布
- [x] 记录详细日志

### 项目配置
- [x] 引用 `Fastdotnet.Plugin.Contracts`
- [x] 在 `ConfigureServices` 中注册处理器
- [x] 使用 `As<IEventHandler<TEvent>>()` 指定接口

---

## 💡 常见问题

### Q1: 为什么找不到 EventAttribute？
**A**: 缺少 using 引用
```csharp
// ❌ 错误
namespace Fastdotnet.PluginA.Events;

// ✅ 正确
using Fastdotnet.Plugin.Contracts.Events;

namespace Fastdotnet.PluginA.Events;
```

### Q2: 事件处理器没有被调用？
**A**: 检查是否注册到 DI 容器
```csharp
// ✅ 必须注册
builder.RegisterType<OrderPaidEventHandler>()
    .As<IEventHandler<PaymentCompletedEvent>>()
    .InstancePerLifetimeScope();
```

### Q3: 如何传递复杂数据？
**A**: 使用 `Data` 字典或自定义属性
```csharp
var @event = new MallOrderCreatedEvent
{
    Data = new Dictionary<string, object>
    {
        ["CustomField"] = someValue
    }
};
```

---

## 🚀 扩展示例

### 添加库存扣减处理器

```csharp
// 新建文件：EventHandlers/InventoryReductionHandler.cs
public class InventoryReductionHandler : IEventHandler<MallOrderCreatedEvent>
{
    public async Task HandleAsync(MallOrderCreatedEvent @event, CancellationToken ct)
    {
        Console.WriteLine($"扣减 {@event.ItemCount} 件商品库存");
        // TODO: 实际的库存扣减逻辑
    }
}

// 注册到 DI 容器
builder.RegisterType<InventoryReductionHandler>()
    .As<IEventHandler<MallOrderCreatedEvent>>()
    .InstancePerLifetimeScope();
```

---

## 📚 参考资料

- [通用事件总线系统架构](../../../fastdotnet-docs/docs/backend/通用事件总线系统架构.md)
- [支付插件事件驱动架构实现计划](../../../fastdotnet-docs/docs/backend/支付插件事件驱动架构实现计划.md)
- [PluginA 事件使用示例](./README.md)

---

*最后更新时间：2026-03-08*  
*PluginA 版本：v1.0.0*  
*状态：✅ 编译通过，可运行*
