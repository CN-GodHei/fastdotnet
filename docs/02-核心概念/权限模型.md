# 权限模型

> 🚧 本文档正在编写中...

## RBAC 权限模型

Fastdotnet 采用基于角色的访问控制（RBAC）：

```
用户 → 角色 → 权限 → 资源
```

## 数据权限

- 行级权限
- 列级权限
- 自定义权限策略

## API 作用域

通过 `ApiUsageScopeEnum` 区分不同端的 API 访问权限：

- `AdminOnly` - 仅管理端
- `AppOnly` - 仅应用端
- `Both` - 两端通用

详见 [架构设计](./架构设计) 中的 API 作用域设计部分。
